package team.unicon.ukrainemetro.localization

/**
 * An 'expect' function to get the platform's current locale.
 * Each platform will provide its 'actual' implementation.
 */
expect fun getPlatformLocale(): Locale